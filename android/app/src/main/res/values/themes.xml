<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema principal do app -->
    <style name="Theme.LifeAI_mobile" parent="Theme.MaterialComponents.NoActionBar.Bridge">
        <!-- Fundo escuro -->
        <item name="android:windowBackground">@color/background_app</item>
        <item name="android:statusBarColor">@color/background_app</item>
        <item name="android:navigationBarColor">@color/background_app</item>
    </style>

    <!-- Tema da Splash Screen -->
    <style name="Theme.LifeAI_mobile.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background_app</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.LifeAI_mobile</item>
    </style>

</resources>